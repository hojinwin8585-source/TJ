# 업데이트 내역 (2026-04-11)

## 변경 파일 2개

### 1. `MSDP/MSDP_v4_20260321_0237.html`
→ 덮어쓰기: `D:\발구지\FINAL MONEY DIGGER\MSDP\MSDP_v4_20260321_0237.html`

### 2. `크롬확장/content.js`
→ 덮어쓰기: 크롬확장 폴더의 `content.js`
→ 적용 후: `chrome://extensions` → 확장 프로그램 새로고침

---

## 다운로드 링크 (Raw)

- MSDP: https://raw.githubusercontent.com/hojinwin8585-source/TJ/claude/add-manual-keyword-input-2iHmE/MSDP/MSDP_v4_20260321_0237.html
- content.js: https://raw.githubusercontent.com/hojinwin8585-source/TJ/claude/add-manual-keyword-input-2iHmE/%ED%81%AC%EB%A1%AC%ED%99%95%EC%9E%A5/content.js

---

## 변경 내용 상세

### A. MSDP — 수동 메인/서브 키워드 입력 기능

**문제**: 원본 타이틀이 이상하게 가져와지면 AI 키워드 생성이 안 됨
**해결**: 메인/서브 키워드를 직접 입력하는 보조수단 추가

#### UI 위치
검색 버튼(1688, 타오바오, 텍스트, 알리바바, 원본) 바로 아래, 슬롯 위
```
[🔍 1688] [🛍 타오바오] [🔤 텍스트] [🌐 알리바바] [🔗원본]
┌──────────────────────────────────────────────────────────┐
│ ✏️ 메인 [메인 키워드 입력]  서브 [서브 키워드 입력]  [적용] [초기화] │
└──────────────────────────────────────────────────────────┘
1  [키워드]  [URL ...]
```

#### 동작
| 기능 | 수동 키워드 없을 때 | 수동 키워드 있을 때 |
|------|-------------------|-------------------|
| AI 키워드 생성 | 원본 타이틀 기반 | 메인+서브 키워드 기반 |
| 타오바오/1688 텍스트 검색 | 원본 타이틀 | 메인+서브 키워드 |
| 알리바바 검색 | 원본 타이틀 | 메인+서브 키워드 |
| 엑셀 내보내기 fallback | 원본 타이틀에서 추출 | 수동 키워드에서 추출 |

#### 사용법
1. 메인 키워드에 핵심 상품명 입력 (예: 여성 원피스)
2. 서브 키워드에 추가 속성 입력 (예: 캐주얼 린넨 여름)
3. [적용] 클릭 → "Liverry " 접두어 붙여서 모든 슬롯 일괄 적용
4. AI 키워드 버튼 → 수동 키워드 기반으로 Claude가 키워드 생성

---

### B. 크롬확장 — 쿠팡/네이버 스크래핑 개선 (content.js v7→v8)

**문제 1**: 쿠팡 1페이지에서 일부 상품만 수집됨
**원인**: lazy-loading — 스크롤 안 하면 상품이 DOM에 로드 안 됨
**해결**: 스크래핑 전 페이지 전체 자동 스크롤 추가

**문제 2**: 사이트별 셀렉터 미분리
**해결**: 쿠팡/네이버 전용 CSS 셀렉터 fallback 추가

#### 주요 변경

1. **사이트 자동 감지**: URL 기반으로 coupang/naver/other 분기
2. **scrollToLoadAll()**: 스크래핑 전 페이지 전체 스크롤 (최대 30회, 안전장치)
3. **쿠팡 전용 셀렉터**: `ul.search-product-list > li`, `.name`, `.price-value`, `.arrival-info` 등
4. **네이버 전용 셀렉터**: `[class*="product_item"]`, `[class*="product_title"]` 등 (해시 클래스 제거)
5. **봇 감지 키워드 확장**: 자동화된 요청, unusual traffic, are you human, 인증이 필요, 보안 인증
6. **이미지 추출 개선**: data-lazy-src, data-image, srcset 속성 대응

#### 스크래핑 흐름 (v8)
```
1. 페이지 접속 → SITE 자동 감지 (coupang/naver/other)
2. AUTO_DETECT → 사이트 전용 셀렉터 우선 → 실패시 범용 auto-detect
3. DO_SCRAPE → 전체 스크롤(lazy-load 로딩) → 데이터 수집
4. 페이지네이션 → 다음 버튼 클릭 → DOM 변화 감지 → 반복
```

---

## 적용 순서

1. 위 다운로드 링크에서 파일 2개 다운로드
2. MSDP html → 로컬 MSDP 폴더에 덮어쓰기
3. content.js → 크롬확장 폴더에 덮어쓰기
4. MSDP: 브라우저에서 Ctrl+Shift+R (강제 새로고침)
5. 크롬확장: chrome://extensions → 새로고침 버튼 클릭
